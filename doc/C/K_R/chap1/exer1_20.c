#include <stdio.h>
int my_getline(char line[], int limit);
int main() {
	return 0;
}

int my_getline(char line[], int limit) {
	int c, i;
	for (i = 0; i < limit -1 && ((c = getchar()) != EOF && c != '\n'; ++i) 
		s[i] = c;
	if (c == '\n')
		s[i] = c;
		++i;
	s[i] = '\0';

	return 0;
}
	
