import numpy as np 
import pandas as pd 

s = pd.Series(np.array([1,2,2,3]), index=['b','c','d', 'e'])
s = pd.Series({'a':5, 'b':8})
s = pd.Series(5, index=)


# rows of data set is called the size of data
# columns of data set is called the dimension of data
# the analysis of single attribute is refered t oas univariage analysis
# the simultaneous analysis of two attribute is called bivariate analysis
# the simultaneous analysis of multiple attribute is called multivariate analysis

